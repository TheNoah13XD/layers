import { Section, Type } from '@components/styled';

const Community = () => {
    return (
        <Section>
            <Type>Your Groups.</Type>
        </Section>
    );
}
 
export default Community;
